class NotificationItem {
  String title;
  String description;
  DateTime dateTime;
  String? tag;

  NotificationItem({
    required this.title,
    required this.description,
    required this.dateTime,
    required this.tag
  });
}